(()=>{'use strict';
const states={supported:{status:'Illustrated status: supported finding',title:'A marked region with linked observations',description:'The intended report would show which images support the same blade region, the estimated shape change, and the uncertainty to review.',action:'inspect the evidence and independently verify the finding.'},insufficient:{status:'Illustrated status: insufficient evidence',title:'Request another useful observation',description:'The intended report would mark the uncertain region, explain the missing coverage or alignment, and suggest an additional view. Automatic viewpoint selection is future research.',action:'acquire the missing evidence before making a geometry or measurement claim.'}};
document.querySelectorAll('[data-future-state]').forEach(button=>button.addEventListener('click',()=>{const state=states[button.dataset.futureState];document.querySelectorAll('[data-future-state]').forEach(b=>b.setAttribute('aria-pressed',String(b===button)));document.getElementById('future-status').textContent=state.status;document.getElementById('future-title').textContent=state.title;document.getElementById('future-description').textContent=state.description;document.getElementById('future-action').replaceChildren(Object.assign(document.createElement('b'),{textContent:'Intended next action: '}),document.createTextNode(state.action));}));
})();
